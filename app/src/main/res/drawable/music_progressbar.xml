<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 背景图 -->
    <item android:id="@android:id/background" android:drawable="@drawable/bar_white" />
    <!-- 第二进度图 -->
    <item android:id="@android:id/secondaryProgress" android:drawable="@drawable/bar_gray" />
    <!-- 进度图 -->
    <item android:id="@android:id/progress" android:drawable="@drawable/bar_red" />
</layer-list>